/** Test-only seam for editor specs that exercise the real timeline stores. */
export {
  makeTimelineTrack,
  resetTimelineCompositionTestState,
  setDefaultRootTimelineTracks,
} from '@/features/timeline/test-helpers'
